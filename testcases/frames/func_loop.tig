let
  function multiply(x: int, y: int): int =
    let
      var result: int := 0
      var i: int := 0
    in
      while i < x do
        (result := result + y; i := i + 1);
      result
    end
in
  multiply(3, 4)
end
