let
  function compute(a: int, b: int, c: int, d: int, e: int, f: int, g: int): int =
    let
      var sum: int := a + b + c
    in
      sum + d + e + f + g
    end
in
  compute(1, 2, 3, 4, 5, 6, 7)
end
